# Core: vocal separation (UVR)
# audio-separator is installed separately by install.sh
# (its diffq dependency needs C compilation — handled by the script)

# torch + torchaudio are installed separately by install.sh
# (GPU version with CUDA) — do NOT add them here.

# Qwen3-ASR transcription
qwen-asr>=0.0.6
transformers>=4.57
accelerate>=1.0
modelscope>=1.35
fireredvad>=0.0.2

# Audio processing
librosa>=0.10
soundfile>=0.12
numpy>=1.26
scipy>=1.11
pydub>=0.25
ffmpy>=1.0

# MTL alignment
g2p_en>=2.0
h5py>=3.10
tqdm>=4.66
sortedcontainers>=2.4

# Metadata & HTTP
mutagen>=1.47
requests>=2.32

# Optional: GUI for Qwen3-aligner
gradio>=6.5
fastapi>=0.129
uvicorn>=0.40
